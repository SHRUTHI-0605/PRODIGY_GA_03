# PRODIGY_GA_03
This project implements a simple text generation algorithm using Markov Chains. The model analyzes a training text and predicts the next word based on the probability of previously occurring words. It builds a statistical model of word transitions and generates new sentences that follow similar patterns as the input text.
